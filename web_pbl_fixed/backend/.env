# ── Server ────────────────────────────────────────
PORT=3000

# ── MongoDB ───────────────────────────────────────
# Replace the URI below with your own MongoDB connection string
# For local MongoDB:  mongodb://localhost:27017/eventhub
# For Atlas:          mongodb+srv://<user>:<password>@cluster.mongodb.net/eventhub
MONGO_URI=mongodb+srv://yaashvi:password11@cluster0.nhfka7f.mongodb.net/?appName=Cluster0

# ── JWT ───────────────────────────────────────────
# Use a long, random, secret string. Never commit this to Git.
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
